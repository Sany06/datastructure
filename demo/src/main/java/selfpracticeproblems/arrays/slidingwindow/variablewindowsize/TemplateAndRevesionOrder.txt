LongestSubarrayWithSumK
MinimumSizeSubarraySumK
Longest Substring Without Repeating Characters
SubarrayProductLessThanK
MaxConsecutiveOnesIII



Frequency Use case
LongestSubstringWithKUniques
SubarraysWithKDistinct


String
Longest Substring Without Repeating Characters
LongestSubstringWithKUniques
MinimumWindowSubString


SUBARRAY PROBLEMS LOGIC
____________________________
| Problem asks for           | Typical approach                                      |
| -------------------------- | ----------------------------------------------------- |
| `< K`, `<= K`, `at most K` | One sliding window                                    |
| `exactly K`                | `atMost(K) - atMost(K-1)`                             |
| `>= K`                     | Often use complement counting (`total - atMost(K-1)`) |


int i = 0, ans = 0;

for (int j = 0; j < n; j++) {

    // do calculation

    while (/* invalid condition */) {
        // shrink window
        i++;
    }

    // if condition satisfied update answer
    //if (product < k)
     // count += (j - i + 1);
}