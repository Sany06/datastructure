############################Revision Sequence#################################
1. Subset Sum
2. Equal sum Partition
3. Count Of Subset Sum
4. PartitionArrayIntoTwoArraysToMinimizeSumDifference
5. PartitionsWithGivenDifference
6. Target Sum

[ Target Sum is same as PartitionsWithGivenDifference]



main code block

for(int i = 1; i <= arr.length ; i++ ) {
    for(int j = 1; j <= sum ; j++) {
        if(arr[i - 1] <= j) {
        dp[i][j] = dp[i - 1][j - arr[i - 1]] || dp[i - 1][j];
        } else {
            dp[i][j] = dp[i - 1][j];
        }
        }
    }
    }
}
}


Note: When we have to return int like return count and similar probs
For initialization do only dp[0][0] = 0

and in the for loop j will start from 0 instead of 1